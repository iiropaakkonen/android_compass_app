<?xml version="1.0" encoding="utf-8"?>
<!--
    A single row in the Nearby Locations list widget.
    setOnClickFillInIntent is called on the root view in NearbyListRemoteViewsService
    so that the launcher merges this intent with the PendingIntentTemplate.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/nearby_item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="5dp"
    android:paddingBottom="5dp"
    android:paddingStart="2dp"
    android:paddingEnd="2dp">

    <ImageView
        android:id="@+id/nearby_item_icon"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:contentDescription="" />

    <TextView
        android:id="@+id/nearby_item_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:textColor="#FFFFFFFF"
        android:textSize="12sp"
        android:maxLines="1"
        android:ellipsize="end" />

    <TextView
        android:id="@+id/nearby_item_distance"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="6dp"
        android:textColor="#C89B3C"
        android:textSize="11sp" />

</LinearLayout>
